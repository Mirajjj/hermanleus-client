import { BrainDirective } from './brain.directive';

describe('BrainDirective', () => {
  it('should create an instance', () => {
    const directive = new BrainDirective();
    expect(directive).toBeTruthy();
  });
});
